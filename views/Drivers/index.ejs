<%- include('../partials/header') %>
<br><br>
<div class="ui cards" href="team/<%= driver.id %> ">
    <% drivers.forEach(driver => { %>
    <div class="card">
        <div class="content">
            <div class="header"><%= driver.name %></div>
            <div class="description">
                <p>Team: <%= driver.team.name %></p>
            </div>
            </div>
    </div>
    <% }) %>
</div>

<%- include('../partials/footer') %>